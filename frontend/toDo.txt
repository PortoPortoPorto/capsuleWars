To Do:

* moveCurrentUnit() function needs to re-render grid with new unit position, also needs to set friendly, enemy and all unit positions in state (study best method)

* When new turn starts, style divs with units to allow hover border effects




Done:

* turn order in global state 
* Hovering over unit displays info in UiRight 
* Current Unit displays info in UiLeft
* End Unit Move functionality, move on to next unit and change turn order 